<li class="me-0" role="presentation">
    <button class="inline-block p-4 border-b-2 rounded-t-lg" id="{{ $target }}"
        data-tabs-target="#{{ $target }}" type="button" role="tab" aria-selected="{{ $select }}">
        {{ $name }}
    </button>
</li>
